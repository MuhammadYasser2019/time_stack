<div id="bar" class= <%= "#{@customer_id}"%> data: {toggle: "barChartModalWindow", target: "#barChartModalWindow"}  >
  <strong>Projects Per Customer</strong>
  <%= bar :bar_chart, @barSize, @bar_data_2.to_json %>
</div>

<div id="barChartModalWindow" class="large reveal" aria-hidden="true" role="dialog" data-reveal)>
</div>

<table>
	<tr>
		<th> Project Name</th>
		<th> User </th>
		<th> Last Logged In </th>
		<th> Sign in Count </th>
	</tr>
	<% %>
	<tr>

	</tr>

</table>

<%#= bar :bar_chart, @barSize, @bar_data_2.to_json %>
<%#= bar :bar_chart, @barSize, @bar_data_3.to_json %>
<%#= pie :pie_chart, @pieSize, @pie_data_2.to_json %>
<%#= h_funnel :h_bar, @barSize, @bar_data_2.to_json %>
